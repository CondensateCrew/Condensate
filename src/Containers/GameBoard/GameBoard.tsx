import React from 'react';

const GameBoard:React.FC = () => {
  return (
    <section>
      <h2>The GameBoard will act as a container,
        based on the route, different Round components will render inside</h2>
    </section>
  )
}
